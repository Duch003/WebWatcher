<UserControl x:Class="WebWatcher.UI.Views.UCMessageBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WebWatcher.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Style="{StaticResource MessageBoxBorderStyle}" Tag="{Binding State}">
            <TextBox x:Name="Message" Style="{StaticResource MessageBoxTextBoxStyle}" Tag="{Binding State}"></TextBox>
        </Border>
    </Grid>
</UserControl>
